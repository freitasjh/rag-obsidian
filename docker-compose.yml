services:
  obsidian-rag:
    build: .
    container_name: obsidian-rag
    environment:
      OBSIDIAN_VAULT_PATH: /data/vault
      OBSIDIAN_VAULT_NAME: brain
    volumes:
      - ./vault:/data/vault
    restart: unless-stopped
